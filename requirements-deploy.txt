# Production image dependencies for the single Render service.
# Keep the direct runtime dependencies pinned so image rebuilds are repeatable.
Flask==3.1.3
Werkzeug==3.1.6
# The Docker image uses OpenCV 4.12, which requires NumPy 2.x.
numpy==2.1.3
gunicorn==22.0.0

# DeepFace's server image uses OpenCV with the required headless Linux runtime
# libraries installed in Dockerfile.
opencv-python==4.12.0.88
tensorflow==2.20.0
keras>=3.12.0,<4
tf-keras==2.20.1
deepface[tensorflow]==0.0.101
