flask==3.1.3
Werkzeug==3.1.6
opencv-python-headless==4.8.1.78
numpy>=1.26.0,<2.2.0
gunicorn==22.0.0
tf-keras

# DeepFace and TensorFlow are required by the current Flask request path.
# start.sh installs them separately; manual setup must install them explicitly:
# python -m pip install deepface tensorflow
